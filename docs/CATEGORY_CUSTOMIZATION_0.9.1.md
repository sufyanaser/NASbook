# NASbook 0.9.1 — Category customization

Primary sidebar folders can be renamed and assigned one of the built-in NASbook category icons. The selected name and icon are persisted in SQLite and are exposed only through the typed preload bridge.

Protected aggregate/system destinations (`All Notes`, `Archive`, and `Trash`) remain fixed to preserve application behavior.

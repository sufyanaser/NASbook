# NASbook 0.9.0 status

Development branch contains the autosave close handshake, database recovery safeguards, regression tests, CI validation, and the Windows NSIS installer pipeline. Promotion to `main` is blocked until GitHub Actions and installed-build close/reopen verification pass.

# Windows build artifact

The `NASbook Windows Release` workflow builds `NASbook Setup 0.9.0.exe` on `windows-latest` after all validation gates pass. The installer is uploaded as the workflow artifact `NASbook-Setup-0.9.0-Windows` and retained for 30 days.

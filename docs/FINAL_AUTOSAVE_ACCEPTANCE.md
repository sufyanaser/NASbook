# Final autosave acceptance

A release is accepted only when edits made immediately before close survive both title-bar close and `Alt+F4`, and the same title/content are present after reopening the installed application.

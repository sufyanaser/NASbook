# Category customization UI

The edit affordance appears only while the navigation rail is expanded. The customization dialog follows the current light/dark theme, supports RTL, validates names, and presents the internal icon catalog as a compact grid.

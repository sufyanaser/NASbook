# Release promotion policy

`develop` may be merged into `main` only after automated validation and the installed-build close/reopen persistence test pass. This prevents unverified builds from replacing the stable branch.
